export class LatencyHistogram{private values:number[]=[];observe(ms:number){if(Number.isFinite(ms))this.values.push(Math.max(0,ms))}percentile(p:number){if(!this.values.length)return 0;const sorted=[...this.values].sort((a,b)=>a-b);const i=Math.min(sorted.length-1,Math.max(0,Math.ceil(p*sorted.length)-1));return sorted[i]}summary(){return{count:this.values.length,p50:this.percentile(.5),p95:this.percentile(.95),p99:this.percentile(.99)}}}
