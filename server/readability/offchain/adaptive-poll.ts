export class AdaptivePoll{private current:number;constructor(private readonly min:number,private readonly max:number,initial:number=min){this.current=Math.max(min,Math.min(max,initial))}increase(factor=1.5){this.current=Math.min(this.max,Math.floor(this.current*factor));return this.current}decrease(factor=0.7){this.current=Math.max(this.min,Math.floor(this.current*factor));return this.current}value(){return this.current}}
