import type {WorkerJob} from './types';
export class JobIndex{private byEvent=new Map<string,string>();private byPhase=new Map<string,Set<string>>();upsert(job:WorkerJob){const prev=this.byEvent.get(job.eventId);if(prev&&prev!==job.jobId)throw new Error('event already indexed to another job');this.byEvent.set(job.eventId,job.jobId);for(const set of this.byPhase.values())set.delete(job.jobId);const set=this.byPhase.get(job.phase)??new Set<string>();set.add(job.jobId);this.byPhase.set(job.phase,set)}jobForEvent(eventId:string){return this.byEvent.get(eventId)}jobsInPhase(phase:string){return [...(this.byPhase.get(phase)??new Set())]}}
