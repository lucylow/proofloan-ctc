import { simulate } from "./proposalSimulator";
export function stressCase020() {
  return { caseId: "stress-020", focus: "governance attack-resistance", proposalWeight: BigInt(1020), notes: ["snapshot", "quorum", "timelock", "constitution"] };
}
