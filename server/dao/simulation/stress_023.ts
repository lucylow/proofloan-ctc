import { simulate } from "./proposalSimulator";
export function stressCase023() {
  return { caseId: "stress-023", focus: "governance attack-resistance", proposalWeight: BigInt(1023), notes: ["snapshot", "quorum", "timelock", "constitution"] };
}
