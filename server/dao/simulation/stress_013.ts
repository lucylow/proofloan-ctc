import { simulate } from "./proposalSimulator";
export function stressCase013() {
  return { caseId: "stress-013", focus: "governance attack-resistance", proposalWeight: BigInt(1013), notes: ["snapshot", "quorum", "timelock", "constitution"] };
}
