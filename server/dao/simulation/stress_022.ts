import { simulate } from "./proposalSimulator";
export function stressCase022() {
  return { caseId: "stress-022", focus: "governance attack-resistance", proposalWeight: BigInt(1022), notes: ["snapshot", "quorum", "timelock", "constitution"] };
}
