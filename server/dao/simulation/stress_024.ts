import { simulate } from "./proposalSimulator";
export function stressCase024() {
  return { caseId: "stress-024", focus: "governance attack-resistance", proposalWeight: BigInt(1024), notes: ["snapshot", "quorum", "timelock", "constitution"] };
}
