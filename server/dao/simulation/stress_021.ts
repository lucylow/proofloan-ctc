import { simulate } from "./proposalSimulator";
export function stressCase021() {
  return { caseId: "stress-021", focus: "governance attack-resistance", proposalWeight: BigInt(1021), notes: ["snapshot", "quorum", "timelock", "constitution"] };
}
