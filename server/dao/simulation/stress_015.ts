import { simulate } from "./proposalSimulator";
export function stressCase015() {
  return { caseId: "stress-015", focus: "governance attack-resistance", proposalWeight: BigInt(1015), notes: ["snapshot", "quorum", "timelock", "constitution"] };
}
