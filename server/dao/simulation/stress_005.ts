import { simulate } from "./proposalSimulator";
export function stressCase005() {
  return { caseId: "stress-005", focus: "governance attack-resistance", proposalWeight: BigInt(1005), notes: ["snapshot", "quorum", "timelock", "constitution"] };
}
