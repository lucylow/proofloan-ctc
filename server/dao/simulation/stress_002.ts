import { simulate } from "./proposalSimulator";
export function stressCase002() {
  return { caseId: "stress-002", focus: "governance attack-resistance", proposalWeight: BigInt(1002), notes: ["snapshot", "quorum", "timelock", "constitution"] };
}
