import { simulate } from "./proposalSimulator";
export function stressCase030() {
  return { caseId: "stress-030", focus: "governance attack-resistance", proposalWeight: BigInt(1030), notes: ["snapshot", "quorum", "timelock", "constitution"] };
}
