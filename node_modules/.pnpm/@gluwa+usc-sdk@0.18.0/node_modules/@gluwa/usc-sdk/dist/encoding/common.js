"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TransactionWithRaw = exports.RawTransactionResponse = void 0;
exports.getTransactionWithRaw = getTransactionWithRaw;
class RawTransactionResponse {
    constructor(authorizationList) {
        this.authorizationList = authorizationList;
    }
}
exports.RawTransactionResponse = RawTransactionResponse;
class TransactionWithRaw {
    constructor(formatted, raw) {
        this.formatted = formatted;
        this.raw = raw;
    }
}
exports.TransactionWithRaw = TransactionWithRaw;
/**
 * Used to extract from the raw transaction JSON the raw yparity values
 * for the authorization list. In case they exists.
 *
 * Wraps the formatted TransactionResponse along with the raw data.
 *
 * @param provider - JsonRpcApiProvider instance
 * @param txHash - Transaction hash
 * @returns TransactionWithRaw or null if not found
 */
function getTransactionWithRaw(provider, txHash) {
    return __awaiter(this, void 0, void 0, function* () {
        var _a;
        let json;
        try {
            json = yield provider.send('eth_getTransactionByHash', [txHash]);
        }
        catch (e) {
            console.error(`Error fetching transaction ${txHash}: ${e.message}`);
            return null;
        }
        if (!json) {
            return null;
        }
        const formattedTx = provider._wrapTransactionResponse(json, yield provider.getNetwork());
        // We map the raw yParity values from the JSON response
        // to a numeric value in RawAuthorization array
        const rawAuthorizationList = ((_a = json.authorizationList) === null || _a === void 0 ? void 0 : _a.map((auth) => ({
            yParity: Number(auth.yParity),
        }))) || null;
        const rawTx = new RawTransactionResponse(rawAuthorizationList);
        return new TransactionWithRaw(formattedTx, rawTx);
    });
}
