import { TransactionReceipt } from 'ethers';
import { TransactionWithRaw } from '../common';
export declare enum EncodingVersion {
    V1 = 1
}
export type EncodingResult = {
    types: string[];
    abi: string;
};
export declare function abiEncode(tx: TransactionWithRaw, rx: TransactionReceipt, encoding?: EncodingVersion): EncodingResult;
