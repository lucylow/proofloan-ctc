"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.QueryBuilder = void 0;
const ethers_1 = require("ethers");
const models_1 = require("./models");
const abi_1 = require("../../encoding/abi");
const QueryBuilderForFunction_1 = require("./QueryBuilderForFunction");
const QueryBuilderForEvent_1 = require("./QueryBuilderForEvent");
const offset_utils_1 = require("./offset-utils");
const encoding_1 = require("../../encoding");
class QueryBuilder {
    constructor(tx, rx, encoding = abi_1.EncodingVersion.V1) {
        this.selectedFields = [];
        this.abiCache = new Map();
        this.tx = tx;
        this.rx = rx;
        this.encoding = encoding;
        // compute the offsets right away.
        let { map, computedOffsets } = (0, offset_utils_1.computeAllOffsets)(this.tx, this.rx, this.encoding);
        this.mappedOffsets = map;
        this.computedOffsets = computedOffsets;
    }
    static createFromTransactionHash(rpc_1, transactionHash_1) {
        return __awaiter(this, arguments, void 0, function* (rpc, transactionHash, encoding = abi_1.EncodingVersion.V1) {
            const tx = yield (0, encoding_1.getTransactionWithRaw)(rpc, transactionHash);
            if (!tx)
                throw new Error(`could not find a transaction by hash ${transactionHash}`);
            const rx = yield rpc.getTransactionReceipt(transactionHash);
            if (!rx)
                throw new Error(`could not find a receipt for the transaction hash ${transactionHash}`);
            return new QueryBuilder(tx, rx, encoding);
        });
    }
    static createFromTransaction(tx, rx, encoding = abi_1.EncodingVersion.V1) {
        return new QueryBuilder(tx, rx, encoding);
    }
    setAbiProvider(provider) {
        this.abiProvider = provider;
    }
    getAbi(contractAddress) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!this.abiProvider) {
                throw new Error('ABI provider is not set');
            }
            if (!this.abiCache.has(contractAddress)) {
                const abi = yield this.abiProvider(contractAddress);
                const iface = new ethers_1.Interface(abi);
                iface.forEachEvent((f, i) => {
                    if (f.name == 'Transfer') {
                        f.topicHash;
                    }
                });
                iface.forEachFunction((f, i) => {
                    if (f.name == 'Transfer') {
                        f.selector;
                    }
                });
                this.abiCache.set(contractAddress, new ethers_1.Interface(abi));
            }
            return this.abiCache.get(contractAddress);
        });
    }
    addStaticField(field) {
        const offset = this.mappedOffsets.get(field);
        if (!offset)
            throw new Error(`Could not find field ${field} in transaction type ${this.tx.formatted.type}`);
        if (offset.isDynamic)
            throw new Error(`Only static fields can be added with this method, ${field} is a dynamic field ${offset.type}`);
        if (!offset.size)
            throw new Error(`offset does not have a size which prevents from being added..`);
        this.selectedFields.push({
            offset: offset.offset,
            size: offset.size,
        });
        return this;
    }
    eventBuilder(eventNameOrSignature, filter, configurator) {
        return __awaiter(this, void 0, void 0, function* () {
            const match = yield this.findEvent(eventNameOrSignature, filter);
            if (!match)
                throw new Error(`could not find an event`);
            // okay now we know where to find it..
            const logs = this.mappedOffsets.get(models_1.QueryableFields.RxLogs);
            if (!logs)
                throw new Error(`could not find a log field inside the decoded transaction, should never happen`);
            // cannot use log.index because thats the index of the log in the whole block :|
            const indexOfLogInTransaction = this.rx.logs.indexOf(match.log);
            const log = logs.children[indexOfLogInTransaction];
            const eventBuilder = new QueryBuilderForEvent_1.QueryBuilderForEvent(log, match.log, match.logDescription);
            configurator(eventBuilder);
            this.selectedFields = [...this.selectedFields, ...eventBuilder.fields];
        });
    }
    multiEventBuilder(eventNameOrSignature, filter, configurator) {
        return __awaiter(this, void 0, void 0, function* () {
            const events = yield this.findAllEvents(eventNameOrSignature, filter);
            if (events.length == 0)
                return;
            // okay now we know where to find it..
            const logs = this.mappedOffsets.get(models_1.QueryableFields.RxLogs);
            if (!logs)
                throw new Error(`could not find a log field inside the decoded transaction`);
            // call configurator for each matched event.
            const combinedFields = events.reduce((prev, event) => {
                const logIndex = this.rx.logs.indexOf(event.log);
                const log = logs.children[logIndex];
                const eventBuilder = new QueryBuilderForEvent_1.QueryBuilderForEvent(log, event.log, event.logDescription);
                configurator(eventBuilder);
                return [...prev, ...eventBuilder.fields];
            }, []);
            // append multi event fields :)
            this.selectedFields = [...this.selectedFields, ...combinedFields];
        });
    }
    addEventSignature(eventNameOrSignature, filter) {
        return __awaiter(this, void 0, void 0, function* () {
            yield this.eventBuilder(eventNameOrSignature, filter, (builder) => {
                builder.addSignature();
            });
        });
    }
    findAllEvents(eventNameOrSignature, filter) {
        return __awaiter(this, void 0, void 0, function* () {
            let logDescriptions;
            if ((0, ethers_1.isHexString)(eventNameOrSignature)) {
                const filteredLogs = this.rx.logs.filter((t) => t.topics[0] == eventNameOrSignature);
                const contractAddresses = Array.from(filteredLogs.reduce((prev, current) => {
                    if (prev.has(current.address))
                        return prev;
                    prev.add(current.address);
                    return prev;
                }, new Set()));
                const abis = yield this.getAbis(contractAddresses);
                logDescriptions = filteredLogs.map((log, logIndex) => {
                    const abi = abis.get(log.address);
                    if (!abi)
                        throw new Error(`missing abi for contract address ${log.address}`);
                    const logDescription = abi.parseLog(log);
                    if (!logDescription)
                        throw new Error(`impossible to parse log with ABI ${logIndex} on contract ${log.address}`);
                    return { log, logDescription };
                });
            }
            else {
                const receiptAbis = yield this.getReceiptAbis();
                logDescriptions = this.rx.logs.map((log, logIndex) => {
                    const abi = receiptAbis.get(log.address);
                    if (!abi)
                        throw new Error(`missing abi for contract address ${log.address}`);
                    const logDescription = abi.parseLog(log);
                    if (!logDescription)
                        throw new Error(`impossible to parse log with ABI at index ${logIndex} on contract ${log.address}`);
                    return { log, logDescription };
                });
            }
            const matches = logDescriptions.filter((pair, index) => {
                // if its a hex string, it means its a event signature, and its already filtered above.
                // if its an event name, try to make sure it matches.
                if (!(0, ethers_1.isHexString)(eventNameOrSignature) && pair.logDescription.name != eventNameOrSignature)
                    return false;
                return filter(pair.log, pair.logDescription, index);
            });
            return matches;
        });
    }
    findEvent(eventNameOrSignature, filter) {
        return __awaiter(this, void 0, void 0, function* () {
            const matches = yield this.findAllEvents(eventNameOrSignature, filter);
            if (matches.length == 0)
                return undefined;
            if (matches.length == 1)
                return matches[0];
            throw new Error(`Ambiguous events matched. (${matches.length} events found)`);
        });
    }
    addEventArgument(eventNameOrSignature, argumentName, filter) {
        return __awaiter(this, void 0, void 0, function* () {
            yield this.eventBuilder(eventNameOrSignature, filter, (b) => b.addArgument(argumentName));
        });
    }
    functionBuilder(functionNameOrSignature, configurator) {
        return __awaiter(this, void 0, void 0, function* () {
            const calldata = this.tx.formatted.data;
            if (calldata == '0x')
                throw new Error(`attempting to add a function argument for a transaction with no calldata`);
            const contractAddress = this.tx.formatted.to;
            if (!contractAddress)
                throw new Error(`there is no to address, which means its not an interaction with a contract, probably a contract deployment?`);
            const contractInteractionAbi = yield this.getAbi(contractAddress);
            const matchedFunction = contractInteractionAbi.getFunction(functionNameOrSignature);
            if (!matchedFunction)
                throw new Error(`Could not find a function with the name or signature ${functionNameOrSignature} in the interface of the contract resolved`);
            let builder = new QueryBuilderForFunction_1.QueryBuilderForFunction(this.mappedOffsets.get(models_1.QueryableFields.TxData), matchedFunction, this.tx.formatted);
            configurator(builder);
            this.selectedFields = [...this.selectedFields, ...builder.fields];
        });
    }
    addFunctionSignature() {
        if (this.tx.formatted.data == '0x')
            throw new Error(`this transaction has no calldata`);
        const dataField = this.mappedOffsets.get(models_1.QueryableFields.TxData);
        this.selectedFields.push({ offset: dataField.offset, size: 4 });
        return this;
    }
    addFunctionArgument(functionNameOrSignature, argumentName) {
        return __awaiter(this, void 0, void 0, function* () {
            yield this.functionBuilder(functionNameOrSignature, (b) => {
                b.addArgument(argumentName);
            });
        });
    }
    getAbis(contractAddresses) {
        return __awaiter(this, void 0, void 0, function* () {
            const abis = contractAddresses.map((contractAddress) => this.getAbi(contractAddress));
            const resultAbis = yield Promise.all(abis);
            const result = new Map();
            contractAddresses.forEach((ca, i) => {
                result.set(ca, resultAbis[i]);
            });
            return result;
        });
    }
    getReceiptAbis() {
        return __awaiter(this, void 0, void 0, function* () {
            const contractAddresses = this.getReceiptContractAddresses();
            return yield this.getAbis(contractAddresses);
        });
    }
    getReceiptContractAddresses() {
        const set = this.rx.logs.reduce((set, log) => {
            if (set.has(log.address))
                return set;
            set.add(log.address);
            return set;
        }, new Set());
        return Array.from(set);
    }
    addManually(offset, size) {
        this.selectedFields.push({
            offset: offset,
            size: size,
        });
    }
    build() {
        return this.selectedFields.map((t) => t);
    }
}
exports.QueryBuilder = QueryBuilder;
