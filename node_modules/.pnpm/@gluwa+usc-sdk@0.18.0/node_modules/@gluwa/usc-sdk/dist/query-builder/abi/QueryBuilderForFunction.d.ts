import { FunctionFragment, TransactionResponse } from 'ethers';
import { FieldMetadata } from './models';
export declare class QueryBuilderForFunction {
    private dataFieldOffset;
    private matchedFunction;
    private tx;
    private cachedCalldataComputed;
    private selectedFields;
    constructor(dataFieldOffset: FieldMetadata, matchedFunction: FunctionFragment, tx: TransactionResponse);
    addSignature(): this;
    addArgument(argumentName: string): this;
    get fields(): {
        offset: number;
        size: number;
    }[];
    private getOffsetsOfCalldataForFunction;
}
