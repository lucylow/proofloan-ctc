import { Log, LogDescription } from 'ethers';
import { FieldMetadata } from './models';
export declare class QueryBuilderForEvent {
    private logOffset;
    private log;
    private logDescription;
    private selectedFields;
    constructor(logOffset: FieldMetadata, log: Log, logDescription: LogDescription);
    addArgument(argumentName: string): this;
    addAddress(): this;
    addSignature(): this;
    get fields(): {
        offset: number;
        size: number;
    }[];
}
