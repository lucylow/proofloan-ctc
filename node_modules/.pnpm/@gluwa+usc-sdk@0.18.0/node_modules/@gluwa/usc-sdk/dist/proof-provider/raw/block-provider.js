"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SimpleBlockProvider = void 0;
const encoding_1 = require("../../encoding");
/**
 * Simple implementation of BlockProvider using an implementation of ethers JsonRpcApiProvider.
 *
 * Has no caching or optimizations.
 */
class SimpleBlockProvider {
    constructor(rpc) {
        this.rpc = rpc;
    }
    getBlockNumber() {
        return this.rpc.getBlockNumber();
    }
    getTransaction(transactionHash) {
        return __awaiter(this, void 0, void 0, function* () {
            return (0, encoding_1.getTransactionWithRaw)(this.rpc, transactionHash);
        });
    }
    getBlockWithReceipts(blockNumber) {
        return __awaiter(this, void 0, void 0, function* () {
            // just slow down a bit to let the RPC cool down.
            yield new Promise((resolve) => setTimeout(resolve, 500));
            // Not in cache, fetch from RPC
            // Get raw block data with transactions prefetched
            let blockDataRaw;
            try {
                blockDataRaw = yield this.rpc.send('eth_getBlockByNumber', [`0x${blockNumber.toString(16)}`, true]);
                if (!blockDataRaw) {
                    console.error(`Block ${blockNumber} not found`);
                    return null;
                }
            }
            catch (e) {
                console.error(`Error fetching block ${blockNumber}: ${e.message}`);
                return null;
            }
            // Wrap transactions into TransactionWithRaw objects
            const transactions = yield Promise.all(blockDataRaw.transactions.map((transaction) => __awaiter(this, void 0, void 0, function* () {
                var _a;
                const formattedTx = this.rpc._wrapTransactionResponse(transaction, yield this.rpc.getNetwork());
                // We map the raw yParity values from the JSON response
                // to a numeric value in RawAuthorization array
                const mappendList = ((_a = transaction.authorizationList) === null || _a === void 0 ? void 0 : _a.map((auth) => ({
                    yParity: Number(auth.yParity),
                }))) || null;
                const rawTx = new encoding_1.RawTransactionResponse(mappendList);
                return new encoding_1.TransactionWithRaw(formattedTx, rawTx);
            })));
            // just slow down a bit to let the RPC cool down.
            yield new Promise((resolve) => setTimeout(resolve, 500));
            // Get raw receipts data
            let recepitsRaw;
            try {
                recepitsRaw = yield this.rpc.send('eth_getBlockReceipts', [`0x${blockNumber.toString(16)}`]);
                if (!recepitsRaw) {
                    console.error(`Receipts for block ${blockNumber} not found`);
                    return null;
                }
            }
            catch (e) {
                console.error(`Error fetching receipts for block ${blockNumber}: ${e.message}`);
                return null;
            }
            // Wrap into ethers objects for use using provider's wrap methods
            const block = this.rpc._wrapBlock(blockDataRaw, true);
            const receipts = recepitsRaw.map((r) => this.rpc._wrapTransactionReceipt(r, this.rpc._network));
            return { block, transactions, receipts };
        });
    }
}
exports.SimpleBlockProvider = SimpleBlockProvider;
