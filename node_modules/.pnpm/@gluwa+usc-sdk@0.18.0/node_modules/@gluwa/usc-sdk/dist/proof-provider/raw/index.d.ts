import { BatchProofResult, ProofResult, ProofProvider } from '..';
import { ChainInfoProvider } from '../../chain-info';
import { EncodingVersion } from '../../encoding';
import { BlockProvider } from './block-provider';
export * as blockProvider from './block-provider';
export { EncodingVersion } from '../../encoding';
/**
 * RawProofBuilder generates raw proofs for a given transaction.
 *
 * It uses a BlockProvider to fetch block and transaction data from the source chain. And a ChainInfoProvider
 * to get chain-specific information needed for proof generation from the attestation chain.
 *
 * The builder constructs Merkle proofs for transactions and continuity proofs for blocks.
 */
export declare class RawProofBuilder implements ProofProvider {
    private blockProvider;
    private chainInfoProvider;
    private chainKey;
    private builder;
    constructor(chainKey: number, blockProvider: BlockProvider, chainInfoProvider: ChainInfoProvider, encoding: EncodingVersion);
    private generateMerkleProofFor;
    getProof(transactionHash: string): Promise<ProofResult>;
    getBatchProof(transactionHashes: string[]): Promise<BatchProofResult>;
}
