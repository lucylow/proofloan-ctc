/**
 * Returns the byte length of a hex string (with or without the `0x` prefix).
 * Returns 0 for nullish values or the empty hex string `"0x"`.
 */
export declare function bytesInHexString(hex?: string | null): number;
