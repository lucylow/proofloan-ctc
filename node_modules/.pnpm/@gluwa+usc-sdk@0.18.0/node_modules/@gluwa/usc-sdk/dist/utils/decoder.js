"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.decodeEvmV1Transaction = decodeEvmV1Transaction;
exports.formatDecodedTransaction = formatDecodedTransaction;
function ensureHexPrefix(hex) {
    if (!hex || typeof hex !== 'string')
        return '0x';
    return hex.startsWith('0x') ? hex : `0x${hex}`;
}
/**
 * Decodes EVM v1 encoded transaction bytes using the on-chain EvmV1Decoder library.
 *
 * @param txBytes - Hex string of encoded transaction (with or without 0x prefix)
 * @param contract - The deployed EvmV1Decoder library contract
 * @param opts - Optional behaviour flags; see {@link DecodeOptions}
 * @returns Decoded transaction with type and full data structure. When
 *          `opts.trackGas` is true the returned object also carries the
 *          aggregate `gasUsed` (sum of estimateGas across all underlying
 *          contract calls).
 */
function decodeEvmV1Transaction(txBytes_1, contract_1) {
    return __awaiter(this, arguments, void 0, function* (txBytes, contract, opts = {}) {
        const encodedBytes = ensureHexPrefix(txBytes);
        if (encodedBytes === '0x' || encodedBytes.length < 4) {
            throw new Error('EvmV1Decoder: Empty or invalid txBytes');
        }
        const trackGas = opts.trackGas === true;
        let gasUsed = BigInt(0);
        // Helper: optionally pair a contract call with its estimateGas and
        // accumulate the estimate into the running total. When tracking is off,
        // behaviour is identical to the previous implementation (single eth_call).
        const callTracked = (fnName, ...args) => __awaiter(this, void 0, void 0, function* () {
            if (!trackGas) {
                return (yield contract.getFunction(fnName)(...args));
            }
            const fn = contract.getFunction(fnName);
            const [value, estimate] = yield Promise.all([fn(...args), fn.estimateGas(...args)]);
            gasUsed += BigInt(estimate);
            return value;
        });
        // 1. Get transaction type (first byte of encoding)
        const txType = yield callTracked('getTransactionType', encodedBytes);
        const typeNum = Number(txType);
        if (typeNum < 0 || typeNum > 4) {
            throw new Error(`EvmV1Decoder: Invalid transaction type ${typeNum}`);
        }
        const isValid = yield callTracked('isValidTransactionType', typeNum);
        if (!isValid) {
            throw new Error(`EvmV1Decoder: Invalid transaction type ${typeNum}`);
        }
        // 2. Decode based on type
        switch (typeNum) {
            case 0: {
                const result = yield callTracked('decodeTransactionType0', encodedBytes);
                return Object.assign({ type: 0, data: normalizeType0(result) }, (trackGas ? { gasUsed } : {}));
            }
            case 1: {
                const result = yield callTracked('decodeTransactionType1', encodedBytes);
                return Object.assign({ type: 1, data: normalizeType1(result) }, (trackGas ? { gasUsed } : {}));
            }
            case 2: {
                const result = yield callTracked('decodeTransactionType2', encodedBytes);
                return Object.assign({ type: 2, data: normalizeType2(result) }, (trackGas ? { gasUsed } : {}));
            }
            case 3: {
                const result = yield callTracked('decodeTransactionType3', encodedBytes);
                return Object.assign({ type: 3, data: normalizeType3(result) }, (trackGas ? { gasUsed } : {}));
            }
            case 4: {
                const result = yield callTracked('decodeTransactionType4', encodedBytes);
                return Object.assign({ type: 4, data: normalizeType4(result) }, (trackGas ? { gasUsed } : {}));
            }
            default:
                throw new Error(`EvmV1Decoder: Unsupported transaction type ${typeNum}`);
        }
    });
}
// --- Normalizers for standalone type-specific field results ---
function normalizeAccessList(raw) {
    return (raw !== null && raw !== void 0 ? raw : []).map((e) => {
        var _a;
        return ({
            account: String((_a = e.account) !== null && _a !== void 0 ? _a : ''),
            storageKeys: Array.isArray(e.storageKeys) ? e.storageKeys.map(String) : [],
        });
    });
}
function normalizeAuthorizationList(raw) {
    return (raw !== null && raw !== void 0 ? raw : []).map((e) => {
        var _a, _b, _c, _d, _e, _f;
        return ({
            chainId: BigInt(String((_a = e.chainId) !== null && _a !== void 0 ? _a : 0)),
            account: String((_b = e.account) !== null && _b !== void 0 ? _b : ''),
            nonce: BigInt(String((_c = e.nonce) !== null && _c !== void 0 ? _c : 0)),
            yParity: Number((_d = e.yParity) !== null && _d !== void 0 ? _d : 0),
            r: BigInt(String((_e = e.r) !== null && _e !== void 0 ? _e : 0)),
            s: BigInt(String((_f = e.s) !== null && _f !== void 0 ? _f : 0)),
        });
    });
}
function normalizeLegacyFields(t) {
    var _a, _b, _c, _d;
    return {
        gasPrice: BigInt(String((_a = t.gasPrice) !== null && _a !== void 0 ? _a : 0)),
        v: BigInt(String((_b = t.v) !== null && _b !== void 0 ? _b : 0)),
        r: String((_c = t.r) !== null && _c !== void 0 ? _c : '0x'),
        s: String((_d = t.s) !== null && _d !== void 0 ? _d : '0x'),
    };
}
function normalizeType1Fields(t) {
    var _a, _b, _c, _d, _e;
    return {
        chainId: BigInt(String((_a = t.chainId) !== null && _a !== void 0 ? _a : 0)),
        gasPrice: BigInt(String((_b = t.gasPrice) !== null && _b !== void 0 ? _b : 0)),
        accessList: normalizeAccessList(t.accessList),
        yParity: Number((_c = t.yParity) !== null && _c !== void 0 ? _c : 0),
        r: String((_d = t.r) !== null && _d !== void 0 ? _d : '0x'),
        s: String((_e = t.s) !== null && _e !== void 0 ? _e : '0x'),
    };
}
function normalizeType2Fields(t) {
    var _a, _b, _c, _d, _e, _f;
    return {
        chainId: BigInt(String((_a = t.chainId) !== null && _a !== void 0 ? _a : 0)),
        maxPriorityFeePerGas: BigInt(String((_b = t.maxPriorityFeePerGas) !== null && _b !== void 0 ? _b : 0)),
        maxFeePerGas: BigInt(String((_c = t.maxFeePerGas) !== null && _c !== void 0 ? _c : 0)),
        accessList: normalizeAccessList(t.accessList),
        yParity: Number((_d = t.yParity) !== null && _d !== void 0 ? _d : 0),
        r: String((_e = t.r) !== null && _e !== void 0 ? _e : '0x'),
        s: String((_f = t.s) !== null && _f !== void 0 ? _f : '0x'),
    };
}
function normalizeType3Fields(t) {
    var _a, _b, _c, _d, _e, _f, _g;
    return {
        chainId: BigInt(String((_a = t.chainId) !== null && _a !== void 0 ? _a : 0)),
        maxPriorityFeePerGas: BigInt(String((_b = t.maxPriorityFeePerGas) !== null && _b !== void 0 ? _b : 0)),
        maxFeePerGas: BigInt(String((_c = t.maxFeePerGas) !== null && _c !== void 0 ? _c : 0)),
        accessList: normalizeAccessList(t.accessList),
        maxFeePerBlobGas: BigInt(String((_d = t.maxFeePerBlobGas) !== null && _d !== void 0 ? _d : 0)),
        blobVersionedHashes: Array.isArray(t.blobVersionedHashes) ? t.blobVersionedHashes.map(String) : [],
        yParity: Number((_e = t.yParity) !== null && _e !== void 0 ? _e : 0),
        r: String((_f = t.r) !== null && _f !== void 0 ? _f : '0x'),
        s: String((_g = t.s) !== null && _g !== void 0 ? _g : '0x'),
    };
}
function normalizeType4Fields(t) {
    var _a, _b, _c, _d, _e, _f;
    return {
        chainId: BigInt(String((_a = t.chainId) !== null && _a !== void 0 ? _a : 0)),
        maxPriorityFeePerGas: BigInt(String((_b = t.maxPriorityFeePerGas) !== null && _b !== void 0 ? _b : 0)),
        maxFeePerGas: BigInt(String((_c = t.maxFeePerGas) !== null && _c !== void 0 ? _c : 0)),
        accessList: normalizeAccessList(t.accessList),
        authorizationList: normalizeAuthorizationList(t.authorizationList),
        yParity: Number((_d = t.yParity) !== null && _d !== void 0 ? _d : 0),
        r: String((_e = t.r) !== null && _e !== void 0 ? _e : '0x'),
        s: String((_f = t.s) !== null && _f !== void 0 ? _f : '0x'),
    };
}
// --- Normalizers for full transaction results ---
function getCommonTx(common) {
    var _a, _b, _c, _d, _e, _f;
    return {
        nonce: BigInt(String((_a = common.nonce) !== null && _a !== void 0 ? _a : 0)),
        gasLimit: BigInt(String((_b = common.gasLimit) !== null && _b !== void 0 ? _b : 0)),
        from: String((_c = common.from) !== null && _c !== void 0 ? _c : ''),
        toIsNull: Boolean(common.toIsNull),
        to: String((_d = common.to) !== null && _d !== void 0 ? _d : ''),
        value: BigInt(String((_e = common.value) !== null && _e !== void 0 ? _e : 0)),
        data: String((_f = common.data) !== null && _f !== void 0 ? _f : '0x'),
    };
}
function getReceipt(receipt) {
    var _a, _b, _c, _d;
    const logs = ((_a = receipt.receiptLogs) !== null && _a !== void 0 ? _a : []);
    return {
        receiptStatus: Number((_b = receipt.receiptStatus) !== null && _b !== void 0 ? _b : 0),
        receiptGasUsed: BigInt(String((_c = receipt.receiptGasUsed) !== null && _c !== void 0 ? _c : 0)),
        receiptLogs: logs.map((l) => {
            var _a, _b;
            return ({
                address_: String((_a = l.address_) !== null && _a !== void 0 ? _a : ''),
                topics: Array.isArray(l.topics) ? l.topics.map(String) : [],
                data: String((_b = l.data) !== null && _b !== void 0 ? _b : '0x'),
            });
        }),
        receiptLogsBloom: String((_d = receipt.receiptLogsBloom) !== null && _d !== void 0 ? _d : '0x'),
    };
}
function extractTuple(result) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k;
    const r = result;
    const common = ((_b = (_a = r.commonTx) !== null && _a !== void 0 ? _a : r[0]) !== null && _b !== void 0 ? _b : {});
    const typeFields = ((_h = (_g = (_f = (_e = (_d = (_c = r.type0) !== null && _c !== void 0 ? _c : r.type1) !== null && _d !== void 0 ? _d : r.type2) !== null && _e !== void 0 ? _e : r.type3) !== null && _f !== void 0 ? _f : r.type4) !== null && _g !== void 0 ? _g : r[1]) !== null && _h !== void 0 ? _h : {});
    const receipt = ((_k = (_j = r.receipt) !== null && _j !== void 0 ? _j : r[2]) !== null && _k !== void 0 ? _k : {});
    return { common, typeFields, receipt };
}
function normalizeType0(result) {
    const { common, typeFields, receipt } = extractTuple(result);
    return {
        commonTx: getCommonTx(common),
        type0: normalizeLegacyFields(typeFields),
        receipt: getReceipt(receipt),
    };
}
function normalizeType1(result) {
    const { common, typeFields, receipt } = extractTuple(result);
    return {
        commonTx: getCommonTx(common),
        type1: normalizeType1Fields(typeFields),
        receipt: getReceipt(receipt),
    };
}
function normalizeType2(result) {
    const { common, typeFields, receipt } = extractTuple(result);
    return {
        commonTx: getCommonTx(common),
        type2: normalizeType2Fields(typeFields),
        receipt: getReceipt(receipt),
    };
}
function normalizeType3(result) {
    const { common, typeFields, receipt } = extractTuple(result);
    return {
        commonTx: getCommonTx(common),
        type3: normalizeType3Fields(typeFields),
        receipt: getReceipt(receipt),
    };
}
function normalizeType4(result) {
    const { common, typeFields, receipt } = extractTuple(result);
    return {
        commonTx: getCommonTx(common),
        type4: normalizeType4Fields(typeFields),
        receipt: getReceipt(receipt),
    };
}
/**
 * Formats a decoded transaction for display (e.g. JSON string).
 */
function formatDecodedTransaction(decoded) {
    const toJson = (obj) => {
        if (obj === null || obj === undefined)
            return obj;
        if (typeof obj === 'bigint')
            return obj.toString();
        if (Array.isArray(obj))
            return obj.map(toJson);
        if (typeof obj === 'object') {
            const out = {};
            for (const [k, v] of Object.entries(obj)) {
                out[k] = toJson(v);
            }
            return out;
        }
        return obj;
    };
    return JSON.stringify({ type: decoded.type, data: toJson(decoded.data) }, null, 2);
}
