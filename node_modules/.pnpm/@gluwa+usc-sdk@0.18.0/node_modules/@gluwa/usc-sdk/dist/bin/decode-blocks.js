"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
const glob_1 = require("glob");
const ethers_1 = require("ethers");
const utils_1 = require("../utils");
const evmV1DecoderAbi_json_1 = __importDefault(require("../utils/evmV1DecoderAbi.json"));
const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
function decodeFromDisk(pathToTxn, contract) {
    return __awaiter(this, void 0, void 0, function* () {
        const encodedData = (0, fs_1.readFileSync)(pathToTxn, {
            encoding: 'utf8',
            flag: 'r',
        }).trim();
        const decoded = yield utils_1.decoder.decodeEvmV1Transaction(encodedData, contract);
        console.log(`     decoded as type ${decoded.type}`);
    });
}
function decodeBlocks(creditcoinUrl, decoderLibraryAddress, pathToStore) {
    return __awaiter(this, void 0, void 0, function* () {
        const contract = new ethers_1.Contract(decoderLibraryAddress, evmV1DecoderAbi_json_1.default, new ethers_1.WebSocketProvider(creditcoinUrl));
        const txnFiles = yield (0, glob_1.glob)('*/**.txt', { cwd: pathToStore, absolute: true });
        txnFiles.sort();
        const numFiles = txnFiles.length;
        console.log(`INFO: found ${numFiles} transactions to decode`);
        if (numFiles === 0) {
            throw new Error('0 files found to decode. Something is wrong. Please investigate');
        }
        for (const [idx, txFile] of txnFiles.entries()) {
            console.log(`>>>> ${idx}/${numFiles} decoding ${txFile}`);
            yield decodeFromDisk(txFile, contract);
            // await sleep(50); // rate-limit ourselves
        }
        console.log(`DONE`);
        process.exit(0);
    });
}
if (process.argv.length < 5) {
    console.error('node dist/bin/decode-blocks.js <ws://creditcoinRpcUrl> <evmV1DecoderAddress> <pathToStore>');
    process.exit(1);
}
const rpcUrl = process.argv[2] || 'ws://127.0.0.1:9944';
const evmV1DecoderAddress = process.argv[3];
const pathToStore = process.argv[4];
decodeBlocks(rpcUrl, evmV1DecoderAddress, pathToStore).catch((reason) => {
    console.error(reason);
    process.exit(1);
});
